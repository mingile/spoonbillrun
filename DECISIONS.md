# 설계 결정 (DECISIONS)

## Vanilla HTML/CSS/JS만 사용

- **선택:** 프레임워크·번들러·npm 없이 단일 페이지.
- **다른 선택지:** React/Vue, Vite 등으로 SPA 구성.
- **이유:** MVP 범위가 지도+목록 연동에 한정되어 있고, Leaflet과 DOM 조작만으로 충분하며 배포·유지가 단순해진다.

## `L.CRS.Simple`과 래스터 지도 이미지

- **선택:** 타일이 아닌 한 장의 이미지를 `L.imageOverlay`로 올리고 `CRS.Simple`로 픽셀 공간을 다룬다.
- **다른 선택지:** 지리 좌표(EPSG:4326) + GeoJSON, 커스텀 CRS, 타일 서버.
- **이유:** “옛 지도 스캔”은 실제 위경도와 1:1이 아닌 경우가 많고, 편집·마커 배치는 픽셀이 자연스럽다.

## 데이터에 Leaflet 좌표가 아닌 이미지 좌표 저장

- **선택:** JSON에는 `{ x, y }` (좌상단 원점)만 저장하고, Leaflet `[lat, lng]`는 `toLatLng()` 한 함수에서만 변환.
- **다른 선택지:** JSON에 lat/lng 저장, 또는 변환을 여러 곳에 분산.
- **이유:** 콘텐츠 작성자는 이미지 도구에서 읽은 픽셀 값을 그대로 쓰면 되고, y축 뒤집기 실수를 한곳으로 모을 수 있다.

## 목록을 현재 지도 화면(viewport) 기준으로 필터링

- **선택:** `moveend` 이후 `map.getBounds().contains()`로 보이는 사진만 목록에 표시, 상단에 “N장 표시 중”.
- **다른 선택지:** 전체 목록 고정, 날짜/태그 필터, 클러스터와 연동한 사이드바.
- **이유:** 지도를 탐색하는 행위와 “지금 이 구역의 사진”이라는 mental model이 맞고, MVP에서 검색·필터 없이도 스케일이 작은 데이터에 유효하다.

## 지도와 목록을 한 화면에서 연동

- **선택:** 데스크톱은 우측 패널, 모바일은 하단 패널; 호버·클릭·선택·상세·`flyTo`를 같은 페이지 상태(`selectedId`, `hoveredId`)로 처리.
- **다른 선택지:** 목록만 별도 페이지, 모달만으로 상세, 지도 없는 갤러리.
- **이유:** “어디에 있었는지”가 핵심 가치라 지도와 목록을 동시에 보는 UX가 목표에 가깝고, 라우팅 없이 상태만으로 MVP 완료 기준을 충족한다.
